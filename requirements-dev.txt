# Test-only dependencies for the unit suite (tests/unit), which runs without
# InkyPi. pillow is here only because nutrislice.py imports PIL.Image for a
# type annotation; no image is actually produced outside the integration suite.
#
# The integration suite (tests/integration) installs InkyPi's own requirements
# instead; see the Testing section of README.md.
pytest>=8.0,<10
pillow>=11.0,<13
